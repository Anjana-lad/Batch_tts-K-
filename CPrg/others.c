// others operators: sizeof(),&,*,?:
#include<stdio.h>
int main(){
    int num;
    float salary;
    char ch;
    double d;
    long double percent;
    printf("size of integer=%d",sizeof(num));
    printf("\n size of float=%d",sizeof(salary));
    printf("\n size of character=%d",sizeof(ch));
    printf("\n size of double=%d",sizeof(d));
    printf("\n size of long double=%d",sizeof(percent));
}